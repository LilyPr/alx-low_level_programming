#include "main.h"
#include <stdio.h>
#include <string.h>

#define DEST_SIZE 40

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char i[98];
	char *ptr;

	char src[] = "Look Here";
	char dest[DEST_SIZE] = "Unimaginable";

	 ptr = _strcpy(s1, "First, solve the problem. Then, write the code\n");
	 printf("%s", i);
         printf("%s", ptr);
	 _strcpy(p, src);
	 printf(dest);

return 0;
}
